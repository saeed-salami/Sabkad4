server:
  port: 8080
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/blog
    username: root
    password:

  jpa:
    hibernate:
      ddl-auto: update
    database-platform: org.hibernate.dialect.MySQL5InnoDBDialect
